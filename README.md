# Bedrock LLaMA API

This is a Node.js + Express API to send PDF patient records to Amazon Bedrock using the LLaMA Maveric model and return layman-friendly explanations.

## Setup

1. Clone the repo:
```bash
git clone <your-repo-url>
cd bedrock-llama-api
